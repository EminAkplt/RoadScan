# Node
node_modules/
# ONNX Runtime Web dosyaları npm install (postinstall) ile üretilir
frontend/vendor/
npm-debug.log*
yarn-error.log*

# Çevre değişkenleri
.env
.env.local

# Yüklenen snapshot görüntüleri (klasör yapısı .gitkeep ile korunur)
backend/uploads/*
!backend/uploads/.gitkeep

# Eğitim (Python venv, veri seti, çıktılar — repoya gitmez; .onnx model dahil edilir)
training/.venv/
training/dataset/
training/dataset_raw/
training/datasets/
training/runs/
runs/
training/*.pt
training/__pycache__/
*.cache

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.tmp
